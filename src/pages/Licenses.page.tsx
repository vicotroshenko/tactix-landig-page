import React from 'react';

import { Layout, LicensesContent } from '../components';

const Licenses = () => {
  return (
    <Layout>
      <LicensesContent />
    </Layout>
  );
};

export default Licenses;
